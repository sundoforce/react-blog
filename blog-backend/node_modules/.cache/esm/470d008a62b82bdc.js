let Koa,Router,bodyParser,mongoose,serve,path,send,api,jwtMiddleware;_62b‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_62b‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_62b‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_62b‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_62b‍.w("koa-static",[["default",["serve"],function(v){serve=v}]]);_62b‍.w("path",[["default",["path"],function(v){path=v}]]);_62b‍.w("koa-send",[["default",["send"],function(v){send=v}]]);_62b‍.w("./api",[["default",["api"],function(v){api=v}]]);_62b‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();








const cors = require('@koa/cors');





// 비구조화 할당을 통하여 process.env 내부 값에 대한 레퍼런스 만들기
const { PORT, MONGO_URI, HOST } = process.env;

mongoose
  .connect(MONGO_URI, { 
    useNewUrlParser: true, 
    useFindAndModify: false,
    useUnifiedTopology: true })
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch(e => {
    _62b‍.g.console.error(e);
  });



const app = new Koa();
app.use(cors());
const router = new Router();


// 라우터 설정
router.use('/api', api.routes()); // api 라우트 적용

// 라우터 적용 전에 bodyParser 적용
app.use(bodyParser());
app.use(jwtMiddleware);


// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());


const buildDirectory = path.resolve(__dirname, '../../blog-frontend/build');
app.use(serve(buildDirectory));
app.use(async ctx => {
  // Not Found이고 , 주소가 /api로 시작하지 않는 경어 
  if (ctx.status === 404 && ctx.path.indexOf('/api') !== 0) {
    // index.html
    await send(ctx, 'index.html', { root: buildDirectory});
  }
})


// PORT 가 지정되어있지 않다면 4000 을 사용
const port = PORT || 4000;
const host = HOST || '0.0.0.0';

app.listen(port, () => {
  _62b‍.g.console.log(HOST)
  _62b‍.g.console.log('Host', host);
  _62b‍.g.console.log('Listening to port %d', port);
  
});
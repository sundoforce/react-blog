let jwt;_dba‍.x([["default",()=>_dba‍.o]]);_dba‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const jwtMiddleware = (ctx, next) => {
    const token = ctx.cookies.get('access_token');
    if (!token) return next();
    try {
        const decoded = jwt.verify(token, process.env.JWT_SECRET);
        _dba‍.g.console.log(decoded);
        return next();
    } catch (e) {
        // 토큰 검증 실패
        return next();
    }
};

_dba‍.d(jwtMiddleware);

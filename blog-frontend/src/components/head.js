import React from 'react';
import styled from 'styled-components';

const headBlock = styled.div``;

const head = () => {
  return (
    <headBlock>
      
    </headBlock>
  );
};

export default head;
